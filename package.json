{
  "name": "tree-handler",
  "description": "module to manipulate tree structures",
  "version": "1.0.9",
  "author": "riexn <riexn.dev@gmail.com>",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "test": "jest --config jestconfig.json --maxWorkers=1 --no-cache",
    "test:w": "jest --config jestconfig.json --maxWorkers=1 --watchAll --no-cache",
    "prepublishOnly": "yarn test",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.0",
    "typescript": "^4.0.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/riexn/tree-handler"
  }
}
